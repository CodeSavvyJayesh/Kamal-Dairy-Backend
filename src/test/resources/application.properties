# Test-only values. Dummy on purpose: the new config makes the app fail fast
# when a secret is missing, and tests must not depend on real credentials.

spring.datasource.url=jdbc:mysql://localhost:3306/kamal_dairy_test
spring.datasource.username=root
spring.datasource.password=test

spring.jpa.hibernate.ddl-auto=none

spring.mail.username=test@example.com
spring.mail.password=test

razorpay.key=rzp_test_dummy
razorpay.secret=dummy_secret

jwt.secret=test-only-secret-that-is-at-least-32-bytes-long-000000
jwt.expiration-ms=3600000

app.cors.allowed-origins=http://localhost:5173
app.contact.recipient=test@example.com
